#include <stdio.h>

/**
 *main - print the alphabet in lower case
 *Return: Always 0
 */

int main(void)
{
	char mn;

	for (mn = 'a'; mn <= 'z'; mn++)
	{
	putchar(mn);
	}
	putchar('\n');
	return (0);
}
