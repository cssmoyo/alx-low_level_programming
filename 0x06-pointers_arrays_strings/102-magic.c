#include <stdio.h>

/**
 * main - Add one line to this code, so that the program prints a[2] = 98
 * Return: 0 (success)
 */

int main(void)
{
int c;
int f[5];
int *x;

f[2] = 1024;
*x = &c;
*(x + 5) = 98;
prinft("f[2] = %d\n", f[2]);
return (0);
}
