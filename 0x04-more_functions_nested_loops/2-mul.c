#include "main.h"

/**
 * mul - multiplies 2 integers
 * @i: first number to multiply
 * @j: second number to multiply with first number
 * Return: The result of multiplication
 */

int mul(int i, int j)

{
	return (i * j);
}
